import { useState, useEffect, Dispatch, SetStateAction } from "react";

function App() {
  const [news, setNews] = useState{null}
  
  const useEffect(()=>{

  }, [])

  return (
   <div>

   </div>
  );
}

export default App;
